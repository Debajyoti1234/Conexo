{
  "version": 2,
  "framework": null,
  "installCommand": "git clone --depth 1 --branch 3.47.0 https://github.com/flutter/flutter.git .flutter",
  "buildCommand": "export PATH=\"$PWD/.flutter/bin:$PATH\" && flutter --version && flutter build web --release --dart-define-from-file=tool/supabase_vercel.json --no-service-worker",
  "outputDirectory": "build/web",
  "rewrites": [
    {
      "source": "/((?!.*\\.).*)",
      "destination": "/index.html"
    }
  ]
}
